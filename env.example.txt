# Copy this to .env.local and fill in values
# Admin authentication password (HMAC-SHA-256 session tokens)
ADMIN_PASSWORD=
# Internal secret for server-to-server calls (workflow engine -> tickets API)
INTERNAL_OPS_SECRET=
# App URL (used by metrics and workflow runner)
NEXT_PUBLIC_APP_URL=http://localhost:3000
# PM2 stats server URL — CF Tunnel URL (reachable from CF Workers edge)
# Local dev override: http://127.0.0.1:9003
PM2_STATS_URL=https://stats.frxncois.com
# Stats server shared secret (x-stats-secret header)
STATS_SECRET=
